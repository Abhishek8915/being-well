import React from "react";

const PomodoroTimer = () => {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <h1 className="text-3xl font-bold">Pomodoro Timer Page</h1>
    </div>
  );
};

export default PomodoroTimer;
